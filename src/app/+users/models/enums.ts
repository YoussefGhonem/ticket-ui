export enum UserAllowedActions {
  Activate = 1,
  Deactivate = 2,
  Unlock = 3
}

export enum IsActiveStatusEnum{
  All = 0,  
  Active = 1,
  InActive = 2,
}

export enum IsLockedStatusEnum{
  All = 0,
  Locked = 1,
  UnLocked = 2,
}